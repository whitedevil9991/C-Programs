#include<stdio.h>
int main()
{
    int a,b;
    printf("enter two numbers:");
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("first number is bigger");
    }
    else if(b>a)
    {
        printf("second number is bigger");
    }
    printf("\nend of the program");
}
