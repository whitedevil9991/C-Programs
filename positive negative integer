#include<stdio.h>
int main()
{
    int num;
    printf("enter a number:");
    scanf("%d",&num);
    if(num>0)
    {
        printf("this is a positive integer");
    }
    else
    {
        printf("this is a negative integer");
    }
}
